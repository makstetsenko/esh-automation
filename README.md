# esh-automation

Application to automate actions in electronic journal "Єдина школа".

Uses playwright to automate users actions in browser.

Uses persist mode to store auth data.

## Usage

### Code generation to help setup actions

```bash
playwright codegen --user-data-dir=./browser-profile https://eschool-ua.com/portal
```
